# STATUS (2026-06-14 — PR B: distributed single-flight LEASE (table + RPCs + algorithm); cross-instance 5→1 proven in-process)
<!-- ocr_cache migration renamed to 20260615000000 (collision fix, PR #143) -->
- PR B IN FLIGHT (branch feat/ocr-distributed-lease off main, includes PR A → PR to main): DISTRIBUTED SINGLE-FLIGHT lease — the cross-instance coordination in-flight dedup couldn't provide. NEW migration 20260615010000_ocr_request_leases.sql — table ocr_request_leases (cache_key_hash PK = sha256 of content-addressed key, NO PII; status in_flight|done|failed; lease_owner; lease_expires_at TTL; provider/model_version/pipeline_version technical only; rate_limited_until/error_class/retry_after_seconds negative cooldown) + 3 atomic SECURITY DEFINER RPCs (acquire_ocr_lease = advisory-xact-locked winner election + EXPIRED-lease steal for crash/stale recovery; complete_ocr_lease owner-checked; fail_ocr_lease owner-checked + cooldown), RLS service-role only, idempotent, NOT applied to prod (coordinator applies). NEW apps/web/src/lib/v1/ocrRequestLease.ts — resolveDistributedDedupMode (OCR_DISTRIBUTED_DEDUP_MODE off|shadow|enforce, default off); LeaseStore interface + InMemoryLeaseStore (faithfully models SQL atomicity) + SupabaseLeaseStore (maps to the RPCs, FAILS CLOSED on DB error → never wins); coordinateProviderCall (winner makes the ONE call + writes cache; losers wait bounded+jittered, NEVER call provider, read winner's cache or get structured unavailable; stale/crash recovery; no busy loop; error NEVER cached as success). 16 tests incl. PROVEN: 5 concurrent identical → 1 winner, 4 waiters, exactly 1 provider call, all 5 get identical result (in-process — the cross-instance PROD proof is PR D). Gates: tsc 0 / 16 lease tests pass. NO runtime wiring (PR C), NO prod flag, NO migration applied to prod.
- CROSS-INSTANCE OCR COORDINATION PROGRAM (owner-directed, supersedes "just cache-shadow"): in-flight dedup is per-instance (canary proved 0 collapses); persistent cache ALONE also insufficient (5 concurrent cache-miss → all 5 call provider before any writes) → need a DISTRIBUTED LEASE. PR order A→B→C→D; do NOT enable OCR_CACHE_MODE=enforce until success-codec-parity + cross-instance 5→1 + cache-mismatch=0 + tenant-isolation + rollback all proven. Vision project quota = OPEN external lever (cache/dedup reduce repeat load, never create quota for new unique docs).
- PR A IN FLIGHT (branch feat/ocr-cache-separate-key-codec off main ca7983b → PR to main): DEDICATED OCR cache key + success-codec parity. NEW apps/web/src/lib/v1/ocrCacheCrypto.ts — OCR_CACHE_ENC_KEY (64 hex, fail-closed, SEPARATE from WIZARD_DRAFT_ENC_KEY by env var) + OCR_CACHE_KEY_VERSION (default '1', bound as AES-256-GCM AAD → version is TAMPER-EVIDENT). sealOcrValue/openOcrValue authenticated; FAIL-CLOSED typed OcrCacheCryptoError(version_mismatch|auth_failed|malformed) — wrong key OR tampered payload = auth_failed (GCM cannot distinguish), every failure → cache MISS + PII-free ocr_cache_security metric (allow-listed, never logs plaintext). NEW success-codec parity test (PROVEN_LOCAL_RECORDED_FIXTURE): full encode→encrypt→store→load→decrypt→decode deep-equal on a synthetic PII-free Cyrillic OcrResult (bbox/confidence-present+absent/warnings/arrays/Unicode); deterministic encoding; fail-closed decode (schema_version_mismatch/binding_mismatch/integrity_failure/corrupt); isCacheable rejects empty+error. HONEST: success parity proven for the codec MATH via recorded fixture, NOT a live provider 200 (Vision 429). Also: corrected the canary evidence wording in docs/reports/OCR_DEDUP_BUDGET_CANARY_2026-06-14.md → ERROR_PATH_PARITY=PROVEN, SUCCESS_RESPONSE_PARITY=UNPROVEN, CROSS_INSTANCE_DEDUP=FAILED. Gates: tsc 0 / 30 new tests pass. NO runtime wiring (store swap is PR C); NO prod flag; NO key created in prod yet.
- P2 OCR DEDUP/BUDGET PRODUCTION CANARY — RAN + ROLLED BACK (prod on proven-OFF baseline; deploy g5tbbw969, code ac3923e, all OCR_* canary flags ABSENT). Owner-authorized 12-step canary of OCR_DEDUP_ENABLED=1 + OCR_BUDGET_MODE=shadow + OCR_BUDGET_DAILY_USD=50 (NO enforce, NO cache). SAFETY PASSED: 5 concurrent identical synthetic (PII-free) → 1 distinct response body; 0 5xx; 0 budget_blocked (shadow never blocks); honest 429 OCR_RATE_LIMITED preserved; requestSha fix CONFIRMED LIVE (gemini_orient vs gemini_vision on the same image → DIFFERENT cache keys → different prompts never collapse); no PII in logs (allow-listed cost events). KEY FINDING: 0 dedup collapses observed — the _inFlight Map is per-lambda-instance and Vercel fans a concurrent burst across instances, so in-flight dedup CANNOT relieve a serverless 429 burst. The real cross-instance lever is the persistent OCR cache (OCR_CACHE_MODE, still OFF — needs separate OCR_CACHE_ENC_KEY + codec verification) + raising the free-tier Vision quota. Rolled back (removed all 3 env vars → redeploy → OFF baseline verified: healthz ok, vision-extract honest 429, pages 307 w/ browser UA). See docs/reports/OCR_DEDUP_BUDGET_CANARY_2026-06-14.md. rollback_tested=yes. requestSha hardening = PR #147 merged (main ac3923e), stays in code (correctness, flag-independent).
- P2 OCR DEDUP-KEY HARDENING IN FLIGHT (branch fix/ocr-dedup-key-bind-prompt off main e0ed338 → PR to main): canary Step-1 precondition. The OCR in-flight dedup / cache key bound only a coarse prompt_version constant, not the actual prompt — at geminiVisionProvider the key bound only the image hash so two concurrent same-image/different-prompt calls would wrongly collapse. FIX: optional `requestSha` (sha256 of the real response-affecting request) added to OcrCacheKeyParts + computeCacheKeySha + the gateway hook + withOcrCostMetrics.gateway, appended to buildOcrCacheKey when present (validated 64-hex; blank/garbage throws). Bound at the 3 image sites: gemini=sha256(prompt), vision=sha256(features+languageHints), docai=sha256("docai:"+mimeType); field-mapper+deepseek already bind full prompt/messages. Back-compat (omit→original 5-part key); zero migration impact (cache OFF, dedup in-flight-only). NO prod flag changed. 6 new key tests. Gates: tsc 0 / full suite 4011 pass 24 skip / ocrCache 11 pass. STEP-1 VERIFIED: 5 paid sites pass meta.gateway; in-flight clears in finally after success+failure; budget shadow never blocks; cache stays off. NEXT: production dedup canary (OCR_DEDUP_ENABLED=1 + OCR_BUDGET_MODE=shadow + OCR_BUDGET_DAILY_USD=50, new deploy, 5-concurrent + 1-different-hash, rollback-ready) → then OCR_CACHE_ENC_KEY + cache-shadow.
- P1 CANONICAL OVERRIDE LOOP IN FLIGHT (branch fix/p1-canonical-override-loop off main 128ea19 → PR to main): closes the orphan override loop the audit flagged. PROBLEM: /api/canonical/[id]/override (append API, 409 optimistic concurrency, INV-11 null) had ZERO callers and canonical_overrides=0 rows; the LIVE user/operator correction path writes only to legacy `user_corrections` via /api/translation/[sessionId]/correct-field + /confirm-field, so resolveCanonicalDocument never saw a human edit → "canonical is the source of truth" was false. FIX: flag-gated DUAL-WRITE. NEW apps/web/src/lib/canonical/overrideLoopMode.ts (flag CANONICAL_OVERRIDE_LOOP = off DEFAULT | shadow | enforce; unknown→off fail-safe; enforce present but NO runtime path consumes it here) + apps/web/src/lib/canonical/overrideLoop.ts (appendCorrectionAsCanonicalOverride: best-effort, NEVER throws to caller — load base, compute expected_version=MAX(version), append a CONFIRMED override via the existing appendCanonicalOverride RPC; INV-11 overrideValue=null preserved as intentional rejection; no-op when value unchanged; PII-free logs). WIRED into correct-field (source user_edit) + confirm-field (ratifies current value) behind the flag: flag OFF → block skipped, legacy write byte-identical to today (canonical_loop:'off'); shadow → legacy still authoritative + canonical override appended; absent/invalid canonical_document_id → legacy-only (canonical_loop:'skipped_no_id', fail-safe). canonical_document_id threaded: review-state route resolves it via getCanonicalDocumentId(sessionId, doc_type) when flag on (null on miss/error), EvidenceReviewPage passes it through FieldCard→confirm + CorrectFieldModal→correct (omitted when null). END-TO-END proven: OCR base → user edit → override appended → resolveCanonicalDocument reflects it (finalValue=override, reviewRequired=false) WITHOUT mutating the immutable base → getCanonicalValue (mapper boundary) reads the corrected value. 13 new tests (flag default OFF + parse; dual-write appends confirmed override; resolve reflects override, base unchanged; INV-11 null; stale version→409 conflict result not throw; not-found→legacy-only; no-op unchanged; no PII in logs; route OFF-parity helper-not-called + legacy write still happens; shadow+id→helper once; shadow no-id→skipped). Gates: tsc 0 real / full suite 4011 pass 24 skip (no decrease, was 3998) / build OK / content-guard 0 / STATUS single H1. PR NOT merged; flag default OFF (prod behaviour unchanged); enforce NOT enabled; base canonical immutable; no secrets/PII.
- P2 OCR RESPONSE CODEC + CACHEABLE-GUARD IN FLIGHT (branch fix/p2-ocr-response-codec off main 9c42ff6 → PR to main): the codec that makes the #143 cache (INERT — no value codec was wired at the call sites) able to actually store+serve a full OCR provider result, and unblocks cache-shadow PARITY. ONE runtime PR. NO prod flag changed; substitution stays gated OFF. NEW apps/web/src/lib/v1/ocrResponseCodec.ts — encodeOcrResult(result, meta) → versioned record {schema_version:1, provider, model, prompt_version, preproc_version, result_json (CANONICAL stable-key-order JSON, deterministic — encoded_at excluded from the body), content_sha256=sha256(result_json), encoded_at}; binds provider·model·prompt_version·preproc_version so a pipeline change invalidates. decodeOcrResult(record, expectedMeta) → FAIL-CLOSED CodecError (treated as a CACHE MISS, never served) on: schema_version mismatch / binding mismatch / integrity failure (content hash) / corrupt-unparseable. HARD RULE isCacheable(result) → true ONLY for a genuine successful read with usable fields; reuses isProviderError/isUnusableOcr to REJECT a provider error (429/5xx/quota/billing/invalid) or BLOCKED, and rejects an EMPTY OcrResult (no raw_text AND no words AND no lines) — encode() itself throws not_cacheable so an error/empty can NEVER be encoded→stored as success. Wired into ocrGateway via a new binding codec form {mode:'ocr_result'}: in OCR_CACHE_MODE=shadow the gateway encodes the LIVE result, stores the first cacheable read (first_seen), and on a later read compares cached-vs-live → emits a PII-free `ocr_cache_parity` {event,key_sha,hit,parity:match|mismatch|first_seen,provider,model} — STILL returns LIVE (NO substitution); a non-cacheable (empty/error) live read emits nothing + stores nothing. In enforce (still OFF in prod) a hit decodes+serves only when binding/integrity pass, else cache_miss → re-read; store-on-miss refuses a non-cacheable value. 36 new tests (round-trip identity; deterministic byte-identical encoding; schema/binding/integrity/corrupt → CodecError; isCacheable rejects empty + all 5 error classes + blocked + malformed; 429/empty NEVER stored; shadow emits parity + still LIVE; first_seen→match; enforce serves decoded hit / re-reads on binding-mismatch; parity events PII-free). Gates: tsc 0 real / full suite 3998 pass 24 skip (no decrease, was 3973) / build OK / content-guard 0 / STATUS single H1. PR NOT merged; NO substitution enabled; NO prod flag changed; 429/errors NEVER cached as success.
- P1 OCR HONEST DEGRADATION IN FLIGHT (branch fix/p1-ocr-honest-degradation off main c8c6ef7 → PR to main): kills the bug where a provider rate-limit was masked as HTTP 200 + fields:[]. ROOT CAUSE (primary-source, docs/audit/VISION_429_DIAGNOSIS.md): Vision SA is on free-tier Google project gen-lang-client-0450386998 with low per-minute limits → intermittent HTTP 429 RATE_QUOTA (transient, NOT a hard cap, NOT billing — the billing-disabled `messenginfo` project is unused; GOOGLE_CLOUD_VISION_API_KEY is invalid but unused). BEFORE: vision-extract returned 200+empty+status="vision_failed:HTTP 429" → wizard advanced as a successful-but-empty read. AFTER: typed OCR errors (NEW apps/web/src/lib/ocr/ocrErrors.ts: classifyProviderError → OCR_RATE_LIMITED|OCR_QUOTA_EXHAUSTED|OCR_PROVIDER_UNAVAILABLE|OCR_BUDGET_EXCEEDED|OCR_INVALID_RESPONSE|OCR_BILLING_DISABLED) carried up from google-vision provider + geminiVisionProvider/documentFieldReader; route FAILS CLOSED with honest 429/503/502 + {ok:false,error_code,retryable,retry_after_seconds?,message}, never 200+empty. Genuine success (ok:core-b2, 200) + honest-empty read (200, P0-502 contract) preserved. NEW retryProvider.ts: bounded (≤3) backoff+jitter, honors Retry-After, retries ONLY transient classes. UI: TranslateWizard shows "recognition temporarily unavailable — try again" + Retry, does NOT advance as a read. SMOKE SPLIT: post-deploy-smoke.yml no longer POSTs a paid doc (now healthz + a no-OCR contract check = malformed request → typed 400); NEW ocr-availability-probe.yml = hourly ONE minimal paid probe, treats transient typed errors as expected. Correctness fix, NO flag (default-on). 48 new tests (classifier/retry/provider-mock 429→typed not 200-empty, billing/quota/timeout, success+honest-empty preserved, route+reader+wizard+smoke source guards). Gates: tsc 0 real; targeted 48 pass + full suite 3973 pass/24 skip (no decrease); build OK; content-guard 0. PR NOT merged.
- P2 OCR CACHE + IN-FLIGHT DEDUP + BUDGET KILL-SWITCH WIRED (branch fix/p2-ocr-cache-budget-wiring off main 77ebe7d → PR to main): the 7-B/7-C step on top of #142's shadow cost emitter. ONE runtime PR. ALL behind flags, DEFAULT OFF, strict OFF-parity (all-off ⇒ byte-identical to today: no lookup/dedup/budget, provider runs exactly as now). NEW apps/web/src/lib/v1/ocrGateway.ts — single chokepoint applying (in order) BUDGET → CACHE → DEDUP around each paid call. Flags (env, default OFF): OCR_CACHE_MODE=off|shadow|enforce, OCR_DEDUP_ENABLED=0|1, OCR_BUDGET_MODE=off|shadow|enforce, OCR_BUDGET_DAILY_USD (manual kill-switch: enforce+0 blocks ALL paid calls instantly). NEW apps/web/src/lib/v1/ocrCacheStoreEncrypted.ts — InMemory + Supabase OCR cache stores; the cache VALUE (OCR result = applicant PII) is sealed AES-256-GCM REUSING wizardDraftCrypto (sealDraft/openDraft) → ciphertext at rest, never logged; KEY = content-addressed sha256(file_sha256·provider·model·prompt_version·preproc_version), NO PII/user/session in key (identical bytes+pipeline ⇒ identical result ⇒ safe cross-user share). Wired via an OPT-IN `meta.gateway` field on withOcrCostMetrics (ocrCostMetrics.ts) at the 5 paid sites: ocr/providers/google-vision.ts, docai/client.ts, docintel/providers/geminiVisionProvider.ts, deepseek/client.ts, ocr/field-mapper.ts. Default gateway hook = runOcrGateway with NO store/codec → provides dedup+budget (flag-gated) but never substitutes (cache substitution needs a value codec a future site supplies). NEW idempotent migration supabase/migrations/20260614020000_ocr_cache.sql (CREATE TABLE IF NOT EXISTS ocr_cache, RLS service-role only, ciphertext columns, TTL via expires_at) — NOT applied to prod (coordinator applies). 31 new tests (cache hit-serves-decrypted/no-provider-call; miss-calls+stores; ciphertext-at-rest asserts no cleartext PII; deterministic content-addressed key; shadow still calls provider+reports hit/miss; dedup collapses 5→1 provider call; budget enforce blocks at cap w/ typed OcrBudgetExceededError; budget shadow never blocks; all-flags-off wrapped===unwrapped; no PII in gateway events). Gates: tsc 0 real / full vitest 3925 pass 24 skip (no decrease, was 3903) / build OK / content-guard 0. PR NOT merged; NO prod flag changed; NO flag enabled in prod.
- (prev) P2 OCR COST OBSERVABILITY IN FLIGHT (branch fix/p2-ocr-cost-metrics-shadow off main 57d16aa → PR to main): OBSERVE-ONLY. NEW apps/web/src/lib/v1/ocrCostMetrics.ts emits PII-free cost/usage events (`ocr_provider_call` + per-upload `ocr_upload_cost_summary`) at EVERY real external provider call site, WITHOUT changing any OCR output/behaviour/retries. Wrapped (non-invasive thunk, result byte-identical): Google Vision (ocr/providers/google-vision.ts), Google DocAI (docai/client.ts), Gemini vision reader (docintel/providers/geminiVisionProvider.ts), DeepSeek chat/reason chokepoint (deepseek/client.ts — covers TPS runBrain + dualOcrCrossref), DeepSeek field-mapper (ocr/field-mapper.ts), Gemini orientation (autoOrient.ts) + Gemini date-boxes (dateRegionRead.ts). Per-upload roll-up via AsyncLocalStorage tally wired into all 4 product OCR routes + translation ocr-from-storage (POST→POST_impl). Confirmed paid-calls/upload: TPS up to 3 (Vision/DocAI + DeepSeek crossref + DeepSeek brain), EAD/Reparole/Translation 1 Gemini read (+Vision crossref/date-ensemble when triggered). Shadow cache_key = sha256(file_sha256·provider·model·prompt_version·preproc_version) — the SAME key the future cache uses, computed now for would-be hit-rate analysis. DEFERRED: 7-B cache substitution (lib/v1/ocrCache/ocrCacheStore), 7-C enforced budget (lib/v1/providerBudget, staging-gated). NO cache lookup/substitution, NO budget enforcement, NO prod flag change in this PR. 37 new tests (PII allow-list, deterministic cache_key, result byte-identical, error path re-throws, per-upload tally, concurrency isolation, static wiring guard). Gates: tsc 0 real / full suite 3903 pass 24 skip (no decrease) / build OK / content-guard 0. PR NOT merged.
- main=ecc4e6c, prod shadow. CLOSED+prod-verified: P0 PII redaction (#135), P1 Re-Parole payment gate (#137), P1 schema-drift diff=0 (#138). Shadow/flag-OFF: env-isolation detect (#136), server-ledger wired into TPS/Re-Parole/Translation wizards (#139/#140/#141, NEXT_PUBLIC_SERVER_LEDGER_ENABLED default OFF, OFF-path parity). Ledger ON proof + Translation V2 E2E + honest benchmark = BLOCKED_EXTERNAL (staging Supabase / Stripe test keys / real corpus).
- P1 RE-PAROLE SERVER-LEDGER WIRING IN FLIGHT (branch fix/p1-reparole-server-ledger-wiring off main 4cfca97 → PR to main): mirrors the TPS wiring just merged (#139). The LIVE wizard ReparoleWizardV2.tsx always wrote PII (field values) to localStorage; the inline hydrate was entangled. This PR FIRST extracted the rebuild into a single applyPersistedDraft(parsed) (one rebuild for both paths), THEN wired the same ledger client (@/lib/v1/wizardLedgerClient, REUSED unmodified) behind flag NEXT_PUBLIC_SERVER_LEDGER_ENABLED (default OFF). When ON: SAVE POSTs draftRecord to /api/wizard-draft (product 'reparole', encrypted at rest), HYDRATE GETs it (+ client isDraftExpired guard) after wiping legacy localStorage keys, terminal-success + restart DELETE it; browser keeps ONLY the opaque httpOnly wizard_draft_token cookie (no PII in localStorage). When OFF: byte-identical localStorage path (every ledger primitive gated on isLedgerClientEnabled(); identical STORAGE_KEY/shape → OFF deploy is a no-op). canonical_document_id carriage (PR #118) preserved in BOTH states (kept in uploadsMeta/draftRecord, rebuilt by applyPersistedDraft). 12 new wiring tests (save→hydrate→clear roundtrip via real /api/wizard-draft route + in-memory Supabase double; browser-jar PII=0; server-row ciphertext-only; TTL drop on hydrate; canonical carriage; static OFF-path parity + applyPersistedDraft-shared asserts on ReparoleWizardV2 source). Playwright live-browser BLOCKED_EXTERNAL (no local Postgres/Docker; staging deploy w/ flag ON + WIZARD_DRAFT_ENC_KEY + wizard_drafts table required) — ON-path proven via integration test like TPS did. Gates: tsc 0 real / full suite (1 pre-existing FAIL in translation/ownerMode.test.ts caused by an UNRELATED uncommitted TranslateWizard.tsx in the working tree — NOT this branch's change; my Reparole-only files are clean) / build OK / content-guard 0. TPS + Translation files UNTOUCHED by this branch. Prod flag UNCHANGED (still OFF → no-op deploy). PR NOT merged.
- (prev) P1 TPS SERVER-LEDGER WIRING IN FLIGHT (branch fix/p1-tps-server-ledger-wiring off main dfba504 → PR to main): the server PII ledger (#129-#133: AES-256-GCM crypto, wizard_drafts, /api/wizard-draft, browser wizardLedgerClient) was wired ONLY into the ORPHAN GeneratePacketBlock.tsx (0 importers) — the LIVE wizard TPSWizardV2.tsx always wrote PII to localStorage. This PR wires the SAME ledger client into TPSWizardV2 behind flag NEXT_PUBLIC_SERVER_LEDGER_ENABLED (default OFF). When ON: persist POSTs the draft to /api/wizard-draft (encrypted at rest), hydrate GETs it, completion+restart DELETE it; browser keeps ONLY the opaque httpOnly wizard_draft_token cookie (no PII in localStorage). When OFF: byte-identical localStorage path (every ledger call gated on isLedgerClientEnabled()). canonical_document_id carriage preserved in BOTH states (kept in uploadsSafe/draftRecord). Orphan GeneratePacketBlock LEFT in place (referenced by controlledBetaLock beta-lock manifest as a path string + content-guard rule 11b — deleting breaks them; dead-but-guarded, zero runtime risk). 11 new wiring tests (save→hydrate→clear roundtrip via real route + in-memory Supabase double; browser-jar PII=0; server-row ciphertext-only; TTL drop on hydrate; canonical carriage; static OFF-path parity asserts on TPSWizardV2 source). Playwright tests/e2e/tps-server-ledger.spec.ts written, test.skip → BLOCKED_EXTERNAL (needs staging deploy with flag ON + WIZARD_DRAFT_ENC_KEY + wizard_drafts table; no local Postgres/Docker, prod flag OFF). Gates: tsc 0 real / full suite 3841 pass 24 skip (no decrease) / build OK / content-guard 0. Prod flag UNCHANGED (still OFF → no-op deploy). PR NOT merged.
- (prev) P1 SCHEMA-DRIFT RECONCILIATION IN FLIGHT (branch fix/p1-schema-drift-reconciliation off main 88e53ca → PR to main): the 5 V2 tables (translation_orders_v2, translation_order_events, document_artifacts, delivery_outbox, stripe_processed_events) + 12 functions + 7 triggers + RLS/policies + private storage bucket translation-artifacts + 2 additive widenings (canonical_overrides source 'operator_override'; PHASE2_TEST_ canonical-guard cleanup) EXIST in prod (applied by frozen PR #119) but were created by NO migration in main → a clean DB from main did not match prod. This PR brings the 4 V2 migration .sql files (20260614000001..000004) into main's supabase/migrations/ as code-of-record, BYTE-IDENTICAL to PR #119 (already authored fully idempotent + additive: CREATE TABLE/INDEX IF NOT EXISTS, CREATE OR REPLACE FUNCTION, DROP TRIGGER/POLICY IF EXISTS, guarded DO-block constraint swap, ON CONFLICT DO NOTHING). Read-only MCP introspection captured prod fingerprint (docs/audit/SCHEMA_DRIFT_PROD_FINGERPRINT.md). DDL-vs-live diff = 0 (62 columns + all constraints/indexes/RLS/policies/triggers/functions/bucket match exactly). Clean-replay CLI NOT run (no local Postgres/Docker; supabase CLI present but cannot reset without a DB) → equivalence proven by DDL-vs-live introspection, NOT a live replay (honest). Ledger naming drift documented (local prefixes 20260614000001.. vs MCP-timestamp ledger versions 20260614005529/005615/005650/032529); recommended owner action = supabase migration repair (NOT executed). Rollback section present per file. MIGRATIONS + DOCS ONLY — NO runtime V2 code. NO prod mutation by agent (read-only only). tsc 0 real / full suite 3830 pass 24 skip / build OK / content-guard 0. PR NOT merged. See docs/audit/SCHEMA_DRIFT_RECONCILIATION.md.
- (prev) P1 Re-Parole payment gate (free-packet bypass closed); PR open, prod unchanged:
- P0 ENV ISOLATION GUARD IN FLIGHT (branch fix/p0-env-isolation-guard off main ca88c2b → PR to main): NEW apps/web/src/lib/env/environmentGuard.ts detects when a non-prod deploy uses the PROD Supabase project (rtfxrlountkoegsseukx) with the RLS-bypassing service-role key. Modes via ENV_ISOLATION_MODE: shadow (default; PII-free console.warn, NEVER throws) / enforce (opt-in throw) / off. Shadow-wired one-time at top of createAdminSupabaseClient() (apps/web/src/lib/supabase/admin.ts) — LOGS only, does NOT gate client creation, cannot throw there; NOT wired to prod startup. 20 tests; tsc 0 real / suite 3815 pass / build OK / content-guard 0. NO Vercel env var removed; prod behaviour UNCHANGED. Full isolation BLOCKED_EXTERNAL (owner must provision staging Supabase + Stripe test keys + test provider keys) — see docs/audit/ENV_ISOLATION_PLAN.md. PR NOT merged.
- P1 RE-PAROLE PAYMENT GATE IN FLIGHT (branch fix/p1-reparole-payment-gate off main 35b4cf7 → PR to main): closes the free-packet bypass. /api/reparole/generate-packet previously generated a paid $15 I-131 packet with ZERO server-side payment verification (client set paid only via ?paid=1). NEW apps/web/src/lib/stripe/requirePaidPacket.ts — shared fail-closed gate: owner-session bypass (same HMAC cookie as TPS) OR X-Payment-Token present (else 403 no_token) → cs_/py_ shape (else 403 bad_token_format) → Stripe payment_status==paid (else 402 unpaid) → metadata.service==product so a TPS token canNOT mint Re-Parole (else 403 wrong_product) → amount_total==REPAROLE_TIER1_PRICE_CENTS (else 403 wrong_amount) → not replayed (else 403, per-instance in-memory store). Stripe error/not-configured → 402 fail-closed. verifyPayment.ts enriched (service/amountTotalCents/sessionId). Gate applied at TOP of reparole route before any generation. Client wired: reparole checkout/success now redirects to wizard ?paid=1&cs=<id>; wizard captures cs + sends x-payment-token. TPS route UNCHANGED (gate is TPS-compatible for later adoption, not refactored now). 15 new tests (route matrix + shared-gate unit) + verifyPayment test updated. tsc 0 real / full suite 3830 pass 24 skip / build OK / content-guard 0. enforce NOT enabled; no real charge; no secrets/PII. PR NOT merged.
- audit branch audit/full-project-reality-2026-06-14; main==prod==02eb595 (live healthz), shadow; ledger flag OFF (/api/wizard-draft=404).
- 23 audit deliverables under docs/audit/ + artifacts/audit/project_truth.json. Synthesis: FULL_PROJECT_AUDIT_2026-06-14.md, CLAIMS_VS_REALITY.csv, RISK_REGISTER.csv, V1_COMPLETION_PLAN_V2.md.
- Verdict: legacy flows real (TPS read+payment, Translation→operator queue). Parallel V1 track (#121-#133) overclaims: ledger NOT_WIRED (orphan component), OCR cache/budget NOT_WIRED, "0 fabricated"/"3/3 readback" UNVERIFIED/PROVEN_LOCAL-via-#116, staging NOT_BUILT.
- P0: raw PII cleartext in tps_ocr_audit; no env isolation. P1: Re-Parole free-packet payment bypass; ledger NOT_WIRED; DB drift (4 V2 migrations only in #119); anti-fab gate OFF on vision; canonical override orphan.
- P0 WRITER FIX IN FLIGHT (branch fix/p0-tps-ocr-audit-pii → PR to main): tps_ocr_audit.brain_raw is now sanitized by apps/web/src/lib/tps/ocrAuditSanitize.ts at the route AND in the writer (defence in depth) — applicant values (source_value/final_value/input_raw/source_line text) dropped, technical keys kept (field/present/confidence/requires_review/inferred/has_source_line/reasons/counts). User-facing OCR result UNCHANGED. Redaction migration 20260614020000_redact_tps_ocr_audit_brain_raw_pii.sql WRITTEN, NOT applied (coordinator applies post-merge; redact-in-place + DB guard trigger). Gates: tsc 0 real / full suite pass / build OK / content-guard 0.
- NOTE: STATUS block below ("PII ledger WIRED/READY", "phases PASS", sha 62c897a) is STALE/overclaiming — see CLAIMS_VS_REALITY.csv. No runtime/env/PR119 change in this audit.

---
## Pre-audit pipeline state — 2026-06-14 (NOTE: claims below are STALE/overclaiming per the audit above; sha 62c897a is outdated, real main=prod=02eb595)

> **This file is current state ONLY.** Machine-readable **verified snapshot**: [RELEASE_STATE.yaml](RELEASE_STATE.yaml)
> (describes main at a basis SHA; CI reports `snapshot_is_stale` — it is NOT a live mirror of main).
> History: [CHANGELOG.md](CHANGELOG.md) and [docs/STATUS_ARCHIVE.md](docs/STATUS_ARCHIVE.md).
> Do not stack historical status blocks here (CI: `scripts/verify-release-state.mjs`).
> **V1 pipeline:** [V1_COMPLETION.yaml](V1_COMPLETION.yaml) · board [V1_STATUS.md](V1_STATUS.md). Phases 1–3 PASS (control plane, dark-code inventory, document registry); **active = GROUND_TRUTH_CORPUS_AND_CACHE** (phase 4). Phase-4 cache half (budget-gated OCR cache) built; ground-truth + paid benchmark remain. Benchmark run: PDF proof 3/3 PASS, recognition 0-fabricated on verified set (I-94 canonical = SAME with correct fixture). BLOCKER: Stripe TEST keys absent (only LIVE) → Stripe Test Mode E2E blocked.
> **PII ledger (crit #9):** server ledger PROVEN E2E + TPS wizard WIRED behind NEXT_PUBLIC_SERVER_LEDGER_ENABLED (default OFF, byte-identical; tsc+build green). READY, NOT verified-live. Re-Parole/Translate hydrate is inline/entangled → need refactor + browser smoke.

## Production
- **production_sha = `62c897a`** = `main` (verified live: `messenginfo.com/api/healthz`).
- Merged & deployed: PR #117 (canonical continuity/persistence), #118 (4-product carriage + product-scoped modes + 404-not-503), #120 (browser-PII containment), #121 (single source of truth: RELEASE_STATE.yaml + guard), **#122 (legacy Translation security: per-action auth + Stripe-re-verified recipient)**.
- All 4 products run the one Document Core (`readDocument → arbitrate → CanonicalDocumentResult`); canonical modes default **shadow** in code. **Live prod env modes (`CANONICAL_MODE_*`) are UNVERIFIED from the repo** — read Vercel to confirm before any enforce.

## Open / in-flight
- **PR #119** (Translation Operator Pipeline V2: canonical-bound orders, artifacts, outbox, state machine) — **OPEN, draft, FROZEN, NOT merged.** Replaces the legacy operator flow but is not in production.
- Browser PII: **containment only** (#120). `value` (localStorage, TPS/Re-Parole) and `raw_cyrillic` (sessionStorage, Translation) still persist. Full removal = Phase B (server ledger) — deferred.

## Open verification gaps (deployed but not fully runtime-proven)
- **Positive paid Translation delivery = RUNTIME_UNVERIFIED.** #122's negative/fail-closed security paths are deployed + prod-smoke-verified (admin routes 404 unauth, raw POST blocked, recipient non-submitting). A successful **hosted Stripe Test Mode** delivery end-to-end has NOT been run — verify on dedicated staging before relying on auto-delivery.

## Blockers (live in production)
- **No dedicated staging:** heavy OCR e2e runs against the production DB + paid providers.

## Next actions (agreed order)
0. **Single source of truth** (this PR) — RELEASE_STATE.yaml + STATUS hygiene.
0.5 **Legacy Translation security hotfix** — per-action auth + server-authoritative recipient (fail-closed).
1. Dedicated staging (staging Supabase + Stripe test mode + test provider keys).
1.5 Dark-code inventory (quality/Brain gates present but OFF — audit before enabling).
2. Private document registry + coverage matrix.
3. Ground-truth corpus + OCR cache (provider+model+prompt+preproc+file_sha256) + hard cost ceiling.
4. Printed-Cyrillic + image-quality benchmark.
5. Brain + dictionaries benchmark.
6. I-821/I-131/I-765 field-by-field generated-PDF proof.
7. Rebase PR #119 onto main → 8. hosted Stripe test E2E → 9. merge #119 → shadow → canary → cutover.
10. Phase B server ledger. 11. Product-scoped enforce (one product at a time). 12. P2 ops. 13. Handwriting (outside v1).

## Hard "do NOT" (standing)
Do not: add a new product · rewrite Canonical Core · enable global enforce · claim handwriting is fully recognized · treat one document as type-coverage · commit real originals/ground-truth-with-PII · keep using production as a test lab · treat test count as user-path proof · let the Brain guess unreadable values.